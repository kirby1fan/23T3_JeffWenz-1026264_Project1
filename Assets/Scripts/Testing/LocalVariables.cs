using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LocalVariables : MonoBehaviour
{
    public string myName = "Jeff";
    public int myHealth = 10;


    // Start is called before the first frame update
    void Start()
    {
        //video seemed straightforward enough...
        //can make something like a temp health to hold the original health to revert back to once something like a poison damage finishes
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
