using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Script1Variables : MonoBehaviour
{

    public int playerHealth = 10; //learned from week 2 video 2.6
    public float playerAttack = 5f; //also week 2 video 2.6. thought this would be more usefull because it will multiply by 1.25
    public string player = "Player";


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
